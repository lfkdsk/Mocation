/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store
.vercel
.wrangler
/.vercel/output

# Scraper working data — the snapshot DB + downloaded images are large binaries
# produced by scripts/scrape. The committed data source (compressed DB / JSON
# shards) and the image assets branch are published separately.
/data/*.sqlite
/data/*.sqlite-*
/data/*.log
/data/assets/
